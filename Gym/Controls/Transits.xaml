<UserControl x:Class="Gym.Controls.Transits"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gym.Controls"
             mc:Ignorable="d" 
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="300" d:DesignWidth="300" Loaded="UserControl_Loaded">
    <ScrollViewer FlowDirection="RightToLeft" VerticalScrollBarVisibility="Hidden">
        <StackPanel Orientation="Vertical" x:Name="List">
            <!--<StackPanel.Style>
                <Style>
                    <Style.Triggers>
                        <Trigger Property="StackPanel.IsMouseOver" Value="true">
                            <Setter Property="StackPanel.Opacity" Value=".8" />
                        </Trigger>
                        <Trigger Property="StackPanel.IsMouseOver" Value="false">
                            <Setter Property="StackPanel.Opacity" Value=".6" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>-->
        </StackPanel>
    </ScrollViewer>
</UserControl>
